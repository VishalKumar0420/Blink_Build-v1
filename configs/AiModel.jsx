const {
  GoogleGenerativeAI,
  HarmCategory,
  HarmBlockThreshold,
} = require("@google/generative-ai");

const apiKey = process.env.NEXT_PUBLIC_GEMINI_API_KEY;
const genAI = new GoogleGenerativeAI(apiKey);

const model = genAI.getGenerativeModel({
  model: "gemini-2.0-flash",
});

const generationConfig = {
  temperature: 1,
  topP: 0.95,
  topK: 40,
  maxOutputTokens: 8192,
  responseMimeType: "text/plain",
};

const codeGenerationConfig = {
  temperature: 1,
  topP: 0.95,
  topK: 40,
  maxOutputTokens: 8192,
  responseMimeType: "application/json",
};

export const chatSession = model.startChat({
  generationConfig,
  history: [],
});

// use to generate file structure and code acc to msg
export const genAicode = model.startChat({
  generationConfig: codeGenerationConfig,
  history: [
    {
      role: "user",
      parts: [{ text: "create budget tracker app" }],
    },
    {
      role: "model",
      parts: [
        {
          text: "Okay, let's outline a plan and the features needed to create a basic budget tracker app.  This will be a simplified overview; a full implementation would require more detailed design choices and coding.\n\n**I. Core Concepts and Features**\n\n*   **Purpose:**  To help users track income and expenses, categorize them, and visualize their financial status to better manage their budget.\n*   **Target Audience:** Individuals who want a simple and easy-to-use tool for personal finance tracking.\n*   **Platform:** (Choose one or more - important decision that drastically alters the approach!)\n    *   **Web App:** Accessible through a web browser (HTML, CSS, JavaScript, possible backend framework like React, Angular, Vue, or Python/Flask/Django). Great for multi-platform use.\n    *   **Mobile App (iOS/Android):** Native apps offer the best performance and can integrate with device features (camera for receipt scanning, etc.). React Native, Flutter, or native Swift/Kotlin.\n    *   **Desktop App:** Can be built with frameworks like Electron (uses web technologies) or native frameworks.\n*   **Key Features:**\n\n    *   **Input/Data Entry:**\n        *   **Income Tracking:** Record income sources (salary, freelance, etc.) with amount, date, and category.\n        *   **Expense Tracking:** Record expenses with amount, date, category, and optional notes/description.\n        *   **Manual Entry:** Allow users to manually input each transaction.\n        *   **Recurring Transactions:**  Set up automatic entries for recurring income/expenses (rent, subscriptions).\n    *   **Categorization:**\n        *   **Predefined Categories:**  Provide a set of common categories (e.g., Food, Housing, Transportation, Entertainment, Utilities).\n        *   **Custom Categories:** Allow users to create and manage their own categories.\n    *   **Reporting/Visualization:**\n        *   **Spending Breakdown:**  Show where the user's money is going (e.g., pie chart showing percentage spent on each category).\n        *   **Income vs. Expenses:**  Display a comparison of income and expenses over time (e.g., bar graph or line chart).\n        *   **Budget Overview:**  Show how much of the budget has been used for each category.\n        *   **Date Range Filtering:** Allow users to view reports for specific time periods (e.g., weekly, monthly, yearly).\n    *   **Budgeting (Optional but Highly Recommended):**\n        *   **Set Budgets:** Allow users to set monthly budgets for each category.\n        *   **Budget Tracking:**  Show progress towards each budget (e.g., \"You've spent $X of your $Y food budget\").\n        *   **Overspending Alerts:**  Notify users when they've exceeded their budget in a category.\n    *   **User Authentication (Important for security):**\n        *   **Login/Registration:** Allow users to create accounts with a username/password.\n        *   **Password Recovery:**  Enable users to reset their passwords.\n    *   **Data Storage:**\n        *   **Local Storage:**  Store data directly on the user's device (simplest, but data is lost if the device is lost or the app is cleared).\n        *   **Cloud Storage:**  Store data in a database in the cloud (e.g., Firebase, AWS, Azure, a dedicated database like PostgreSQL or MongoDB).  Allows for data synchronization across devices.\n    *   **User Interface (UI):**\n        *   **Clean and intuitive design:**  Easy to navigate and understand.\n        *   **Mobile-friendly (if applicable):**  Responsive layout that adapts to different screen sizes.\n\n**II.  Technology Stack (Examples - adjust based on your skills/experience)**\n\n*   **Web App:**\n    *   **Frontend:** HTML, CSS, JavaScript, React (or Angular, Vue)\n    *   **Backend:** Python (Flask or Django), Node.js (Express), PHP (Laravel)\n    *   **Database:** PostgreSQL, MySQL, MongoDB (Cloud: Firebase, AWS, Azure)\n*   **Mobile App (React Native):**\n    *   **Frontend:** React Native (JavaScript/JSX)\n    *   **Backend:** (Optional, if using cloud storage)  Node.js (Express), Python (Flask/Django)\n    *   **Database:** Firebase, AWS, Azure, Realm\n*   **Mobile App (Flutter):**\n    *   **Frontend:** Dart, Flutter framework\n    *   **Backend:** (Optional, if using cloud storage) Node.js (Express), Python (Flask/Django)\n    *   **Database:** Firebase, AWS, Azure, Hive (local), SQLite (local)\n\n**III.  Development Steps (Simplified)**\n\n1.  **Planning & Design:**\n    *   **Define Scope:**  Decide on the core features for your Minimum Viable Product (MVP).\n    *   **UI/UX Design:**  Sketch out the user interface (wireframes) and how users will interact with the app.  Think about the user flow.\n    *   **Data Model:**  Define the structure of your data (e.g., how income/expense records will be stored, categories, user accounts).\n2.  **Setup Development Environment:**\n    *   Install necessary tools (e.g., IDE, Node.js, Python, database software).\n    *   Create a new project in your chosen framework (e.g., `create-react-app`, `flutter create`).\n3.  **Build the User Interface:**\n    *   Create the basic layout of the app.\n    *   Implement forms for adding income and expenses.\n    *   Create UI elements for displaying reports (charts, tables).\n4.  **Implement Data Handling:**\n    *   Implement functions to store and retrieve data (either locally or from a database).\n    *   Implement logic to categorize transactions.\n5.  **Implement Reporting Logic:**\n    *   Write code to generate reports based on the stored data.\n    *   Use charting libraries (e.g., Chart.js, Recharts, Flutter's built-in charts) to visualize the data.\n6.  **Implement User Authentication (if needed):**\n    *   Add registration and login functionality.\n    *   Secure user passwords (use hashing and salting).\n7.  **Testing:**\n    *   Thoroughly test all features of the app.\n    *   Fix bugs and make improvements.\n8.  **Deployment:**\n    *   Deploy the app to a web server or app store (if applicable).\n\n**IV. Code Examples (Illustrative - React/JavaScript)**\n\nThis is a simplified example to give you a flavor of the code.  It's incomplete and requires further implementation.\n\n```javascript\n// Example React component for adding an expense\nimport React, { useState } from 'react';\n\nfunction AddExpenseForm({ onAddExpense }) {\n  const [amount, setAmount] = useState('');\n  const [category, setCategory] = useState('');\n  const [date, setDate] = useState('');\n  const [description, setDescription] = useState('');\n\n  const handleSubmit = (e) => {\n    e.preventDefault();\n    const newExpense = {\n      amount: parseFloat(amount),\n      category,\n      date,\n      description,\n    };\n    onAddExpense(newExpense); // Callback to parent component\n    setAmount('');\n    setCategory('');\n    setDate('');\n    setDescription('');\n  };\n\n  return (\n    <form onSubmit={handleSubmit}>\n      <label>\n        Amount:\n        <input\n          type=\"number\"\n          value={amount}\n          onChange={(e) => setAmount(e.target.value)}\n          required\n        />\n      </label>\n      <label>\n        Category:\n        <select value={category} onChange={(e) => setCategory(e.target.value)} required>\n          <option value=\"\">Select Category</option>\n          <option value=\"Food\">Food</option>\n          <option value=\"Housing\">Housing</option>\n          {/* Add more categories */}\n        </select>\n      </label>\n      <label>\n        Date:\n        <input\n          type=\"date\"\n          value={date}\n          onChange={(e) => setDate(e.target.value)}\n          required\n        />\n      </label>\n       <label>\n        Description:\n        <input\n          type=\"text\"\n          value={description}\n          onChange={(e) => setDescription(e.target.value)}\n        />\n      </label>\n      <button type=\"submit\">Add Expense</button>\n    </form>\n  );\n}\n\nexport default AddExpenseForm;\n```\n\n```javascript\n// Example component to display expense list\nimport React from 'react';\n\nfunction ExpenseList({ expenses }) {\n    return (\n        <div>\n            <h2>Expenses</h2>\n            <ul>\n                {expenses.map((expense, index) => (\n                    <li key={index}>\n                        {expense.date} - {expense.category}: ${expense.amount} - {expense.description}\n                    </li>\n                ))}\n            </ul>\n        </div>\n    );\n}\n\nexport default ExpenseList;\n```\n\n**V. Important Considerations:**\n\n*   **Security:** Protect user data (especially passwords). Use secure coding practices to prevent vulnerabilities.\n*   **Accessibility:** Make the app usable for people with disabilities.\n*   **Scalability:** If you anticipate a large number of users, design the app with scalability in mind.\n*   **Error Handling:** Implement proper error handling to gracefully handle unexpected situations.\n*   **Testing:** Write unit tests, integration tests, and end-to-end tests to ensure the app is working correctly.\n*   **User Feedback:**  Gather feedback from users to improve the app.\n\n**VI.  Where to Start:**\n\n1.  **Choose a platform and technology stack:** Base this on your skills and the target audience.\n2.  **Start with a very basic MVP:** Focus on the core functionality (inputting income/expenses and viewing a simple report).  Don't try to build everything at once.\n3.  **Iterate and add features gradually:** Based on user feedback and your priorities.\n\nThis comprehensive overview should get you started on building your budget tracker app! Remember to break down the project into smaller, manageable tasks and focus on incremental development. Good luck!\n",
        },
      ],
    },
  ],
});
